name: Simple Pipeline

on:
  push:
    branches:
      - '*'

jobs:
  display_message:
    runs-on: ubuntu-latest

    steps:
      - name: Display message
        run: echo "Hello from GitHub Actions!"
